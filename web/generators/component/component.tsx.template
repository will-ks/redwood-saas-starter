import { FC } from 'react'

const ${pascalName}: FC = () => {
  return (
    <div>
      <h2>{'${pascalName}'}</h2>
      <p>{'Find me in ${outputPath}'}</p>
    </div>
  )
}

export default ${pascalName}
